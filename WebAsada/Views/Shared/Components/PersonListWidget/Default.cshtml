@model WebAsada.ViewModels.EstateVM

<table class="table table-fluid table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Identificación</th>
        </tr>
    </thead>
    <tbody>
        @for (var i = 0; i < Model.Owners.Count(); i++)
        {
            <tr>
                @Html.HiddenFor(x => Model.Owners[i].Id)
                <td>
                    @Html.TextBoxFor(x => Model.Owners[i].DisplayValue, new { @class = "form-control", @readonly = "readonly" })
                </td>
                <td>
                    @Html.TextBoxFor(x => Model.Owners[i].DisplayValue, new { @class = "form-control", @readonly = "readonly" })
                </td> 
            </tr> 
        }
    </tbody>
</table>